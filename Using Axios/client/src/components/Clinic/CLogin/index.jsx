import React from 'react';
import ClinicLogin from './ClinicLogin';

export const CLogin = ({contract}) => {
  return (
    <div>
        <ClinicLogin contract = {contract}/>
    </div>
  )
}

export default CLogin;